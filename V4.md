# AI Model Explorer V4 Architecture Upgrade & Implementation Specification

## 1. 项目定位

AI Model Explorer 是一个面向普通用户、开发者和 AI 学习者的大模型导航与 API 选型助手。

V4 版本目标：

将现有 V3 模型展示网站升级为：

> 一个结构化的大模型知识数据库，为未来模型推荐 Agent、智能 Router、成本优化和 API 调度提供基础数据。

注意：

V4 不是重新开发项目。

必须基于现有 V3：

* 保留已有功能
* 保留现有技术栈
* 增量升级
* 避免破坏已有资产

---

# 2. 核心设计原则

## 2.1 不追求模型数量

AI Model Explorer 不做模型大全。

目标：

精选主流、有实际使用价值的模型。

收录标准：

必须满足：

* 有明确厂商/组织
* 有明确模型名称和版本
* 有官方文档、API 或部署方式
* 有实际学习或开发价值

不收录：

* 无明确来源模型
* 无文档模型
* 临时测试模型
* 重复套壳模型

---

# 3. 模型成本体系

模型按照用户成长阶段分类：

## Free

免费体验模型。

适合：

* AI 入门
* API 学习
* Demo 测试

## Cost Effective

低成本开发模型。

适合：

* 个人项目
* Agent 实验
* 小规模应用

## Production

标准商业模型。

适合：

* 企业应用
* 高质量生产环境

注意：

成本等级和模型能力分开设计。

---

# 4. 模型访问方式分类

模型访问方式：

## Official API

官方 API。

## Free API

免费额度 API。

## Open Source

开放权重模型。

## Self Host

支持私有部署。

---

# 5. 数据架构升级

V4 不再使用单一 models.json 作为全部数据。

升级为：

```
data/

├── providers.json
├── models.json
├── gateways.json
├── api_access.json
├── pricing.json
└── scenarios.json
```

---

# 6. 数据关系设计

整体关系：

```
Model Provider
        |
        |
      Model
        |
        |
   API Access
        |
        |
    Gateway
```

---

# 7. Provider 数据

表示模型研发厂商。

例如：

* OpenAI
* Anthropic
* Google
* DeepSeek
* Alibaba
* Zhipu
* Moonshot
* Meta

字段：

```json
{
"id":"openai",
"name":"OpenAI",
"type":"model_provider",
"country":"USA"
}
```

---

# 8. Model 数据

模型核心信息。

必须包含：

## Identity

模型身份：

* name
* version
* provider
* aliases

支持：

中文名称、简称、历史名称搜索。

例如：

```
Qwen
通义千问
千问
```

均可匹配。

---

## Role

模型定位：

例如：

* general_model
* reasoning_model
* coding_model
* agent_model
* multimodal_model
* low_cost_model

---

## Capability

技术能力：

例如：

* coding
* reasoning
* vision
* audio
* video
* tool_use
* function_calling
* long_context
* structured_output
* multilingual

---

# 9. 三层分类体系

采用：

```
Scene
  ↓
Role
  ↓
Capability
```

---

## Scene（用户场景）

面向普通用户：

例如：

* 写代码
* 开发 AI Agent
* 学习 AI
* 内容创作
* 文档分析
* 图片生成
* 视频生成
* 本地部署
* 寻找低成本 API

---

## Role（模型定位）

面向开发者：

例如：

* 通用模型
* 推理模型
* 代码模型
* Agent 模型
* 多模态模型

---

## Capability（技术能力）

面向系统：

用于未来：

* 推荐 Agent
* Router

---

# 10. API 架构设计

API 不作为模型简单字段。

独立设计。

原因：

同一个模型可能有：

* 官方 API
* 第三方 Gateway
* OpenAI Compatible API

---

## API Access 数据

记录：

* API Provider
* Base URL
* Model ID
* Protocol
* Authentication Type
* Supported Features
* Pricing

例如：

```
Claude Sonnet

↓

Anthropic API

↓

claude-sonnet-x

↓

支持:

Streaming
Tool Calling
```

---

禁止：

保存：

* API Key
* 用户密钥

---

# 11. Gateway 设计

Gateway 表示调用平台。

例如：

* OpenRouter
* Groq
* Together AI
* NVIDIA NIM
* SiliconFlow
* New API

注意：

Gateway 不是模型厂商。

例如：

错误：

```
GPT-5 Provider = OpenRouter
```

正确：

```
GPT-5 Provider = OpenAI

Available Gateway:

OpenAI API
OpenRouter
```

---

# 12. 首页设计

原则：

不是重做首页。

采用：

模块化追加。

保持 V3 功能。

新增模块：

---

## 搜索入口（核心）

支持：

* 模型名称
* 中文名称
* 别名
* 厂商
* 能力
* 使用场景

---

## Scene 推荐模块

新增：

“你想用 AI 做什么？”

例如：

* 写代码
* 开发 Agent
* 学习 AI
* 找免费模型

该模块独立组件化。

未来可调整位置。

---

## 热门模型模块

不做排行榜。

改为：

“当前主流模型”。

展示：

* GPT
* Claude
* Gemini
* DeepSeek
* Qwen

---

## 成本入口

展示：

* 免费模型
* 低成本模型
* 商业模型

---

## API 快速入口

帮助用户：

知道：

“这个模型怎么调用”。

---

# 13. 模型详情页设计

采用三层展示。

---

## 第一层：普通用户

展示：

* 模型名称
* 厂商
* 一句话介绍
* 标签
* 推荐用途

---

## 第二层：开发者

展示：

* Context
* Pricing
* API方式
* Model ID

---

## 第三层：工程信息

展示：

* Endpoint
* Protocol
* Tool Calling
* Streaming
* Structured Output

---

# 14. V4 当前不开发内容

暂不实现：

* 自动排行榜
* 自动 Benchmark
* 自动爬虫
* 在线聊天功能
* 用户系统
* 社区功能
* 自动 Agent 推荐

原因：

保持项目核心：

模型导航 + API 选型。

---

# 15. Workbuddy 执行流程

## Step 1：项目审计

不要修改代码。

先输出：

```
PROJECT_AUDIT_V4.md
```

包含：

* 当前项目结构
* 当前数据结构
* 当前功能
* V4差距分析
* 风险点

---

## Step 2：数据层升级

顺序：

```
旧数据

↓

新数据结构

↓

迁移脚本

↓

数据验证

```

---

## Step 3：前端适配

读取新的数据结构。

保持已有页面。

---

## Step 4：新增模块

逐步增加：

* Scene 推荐
* API 信息展示
* 成本筛选
* 调用方式展示

---

# 16. V4 验收标准

用户可以：

✅ 搜索模型
✅ 中文/别名搜索
✅ 按用途寻找模型
✅ 查看模型定位
✅ 查看成本等级
✅ 查看 API 调用方式
✅ 查看 Model ID
✅ 找免费模型
✅ 找低成本模型

开发者可以：

✅ 获取模型信息
✅ 获取 API 信息
✅ 判断模型能力
✅ 判断调用方式

架构支持未来：

✅ AI 推荐 Agent
✅ Model Router
✅ New API 集成

---

# 最终目标

AI Model Explorer V4 不只是一个模型展示网站。

它应该成为：

```
AI 模型知识数据库

        ↓

模型选择系统

        ↓

AI 推荐 Agent

        ↓

智能 Router

        ↓

模型调用基础设施
```

---

End.
